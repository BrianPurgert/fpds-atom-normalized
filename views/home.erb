<div class="alert-banner">
  <div class="container">
    ⚠️ <strong>Notice:</strong> FPDS.gov ezSearch was decommissioned on February 24, 2026. This site preserves federal procurement data from the FPDS Atom feed.
    Data is now officially available at <a href="https://sam.gov/fpds" target="_blank">SAM.gov/fpds</a>.
  </div>
</div>

<section class="hero">
  <div class="container">
    <h1>Federal Procurement <span>Data System</span></h1>
    <p>Search federal contract awards, vendors, and agencies. Preserved from the FPDS.gov Atom feed before decommissioning.</p>
    <div class="search-hero">
      <form action="/search" method="get">
        <input type="text" name="q" placeholder="Search contracts by keyword, PIID, vendor, or agency..." autocomplete="off">
        <button type="submit">Search</button>
      </form>
      <div class="search-hint">Try: vendor name, contract number (PIID), agency name, or description keywords</div>
    </div>
  </div>
</section>

<section class="container">
  <div class="stats-grid">
    <div class="stat-card">
      <div class="stat-value"><%= stats[:contracts] ? stats[:contracts].to_s.reverse.gsub(/(\d{3})(?=\d)/, '\\1,').reverse : '—' %></div>
      <div class="stat-label">Contract Actions</div>
    </div>
    <div class="stat-card">
      <div class="stat-value"><%= stats[:vendors] ? stats[:vendors].to_s.reverse.gsub(/(\d{3})(?=\d)/, '\\1,').reverse : '—' %></div>
      <div class="stat-label">Vendors</div>
    </div>
    <div class="stat-card">
      <div class="stat-value"><%= stats[:agencies] ? stats[:agencies].to_s.reverse.gsub(/(\d{3})(?=\d)/, '\\1,').reverse : '—' %></div>
      <div class="stat-label">Agencies</div>
    </div>
    <div class="stat-card">
      <div class="stat-value"><%= stats[:total_obligated] ? format_currency(stats[:total_obligated]) : '—' %></div>
      <div class="stat-label">Total Obligated</div>
    </div>
  </div>
</section>

<section class="container" style="padding-bottom: 60px;">
  <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 24px;">
    <div class="help-card">
      <h2>🔍 ezSearch</h2>
      <p>Search federal contract awards using keywords, filters, and advanced criteria. Find contracts by PIID, vendor name, agency, NAICS code, or dollar amount.</p>
      <a href="/search" style="font-weight:600;">Start Searching →</a>
    </div>
    <div class="help-card">
      <h2>📊 Reports</h2>
      <p>View top agencies, vendors, and industry codes by obligated amounts. Explore procurement trends and spending patterns across the federal government.</p>
      <a href="/reports" style="font-weight:600;">View Reports →</a>
    </div>
    <div class="help-card">
      <h2>🔗 JSON API</h2>
      <p>Access contract data programmatically via our JSON API endpoint. Build applications and integrations with federal procurement data.</p>
      <a href="/help#api" style="font-weight:600;">API Documentation →</a>
    </div>
  </div>

  <% if stats[:earliest_date] && stats[:latest_date] %>
  <div style="text-align:center; margin-top:32px; color:#868e96; font-size:0.85rem;">
    Data coverage: <%= format_datetime(stats[:earliest_date]) %> — <%= format_datetime(stats[:latest_date]) %>
  </div>
  <% end %>
</section>
